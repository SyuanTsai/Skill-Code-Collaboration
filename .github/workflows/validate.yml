name: Validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  catalog:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Code Collaboration catalog
        shell: pwsh
        run: ./tests/validate-catalog.ps1
